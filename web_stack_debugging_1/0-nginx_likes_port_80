#!/usr/bin/env bash
# Fix Nginx so it listens on port 80

apt-get update -y
apt-get install -y nginx

# Kill ONLY real nginx workers & master
for pid in $(ps aux | grep "nginx: " | grep -v "grep" | awk '{print $2}'); do
    kill -9 "$pid"
done

rm -f /var/run/nginx.pid
service nginx start
