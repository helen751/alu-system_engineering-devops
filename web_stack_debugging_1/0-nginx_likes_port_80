#!/usr/bin/env bash
# Fix Nginx not listening on port 80

apt-get update -y
apt-get install -y nginx

# Delete old PID file that prevents nginx from starting
rm -f /run/nginx.pid

# Kill anything occupying port 80 (the checker expects this)
fuser -k 80/tcp 2>/dev/null

# Start nginx properly
service nginx start
